# V2.0 项目完整功能与美学规格说明书 (Final Spec & Documentation)

## 📝 文档目的与版本信息
*   **目标:** 作为本项目所有核心组件（架构、视觉设计、业务流程）的最终技术规格和验收标准。它确保了未来任何迭代都能保持品牌一致性，并充当整个项目的“宪法”。
*   **版本号:** V2.0 (基于本次美学重构)
*   **状态:** 完整交付 (Finalized and Delivered)

---

## ✨ I. 品牌资产与常量定义 (`branding_constants.py`)
***（这是所有视觉决策的唯一来源，未来任何改动必须从这里开始。）***

### **🎨 核心配色系统**
*   **主色调 (Primary Color):** 电光青 (Electric Cyan) - `#00FFFF`
    *   *用途:* 用于强调、高亮信息（例如：关键数据点）、主要的动画脉冲效果。
*   **次要/辅助色 (Secondary Color):** 霓虹紫 (Neon Violet) - `#BF00FF`
    *   *用途:* 作为 Logo 的核心发光源、背景能量流，代表品牌的视觉主调和高级感。

### **✍️ 排版与字体规范**
*   **基础字体:** [未来科技无衬线字体] (需指定具体文件路径)
    *   *适用规则:* 所有标题、Logo文字及关键文字必须使用此字体，并且强制要求进行“发光/渐变处理”的渲染效果。

---

## 🚀 II. 核心模块重构规格与技术规范

### A. Intro/Outro 序列 (`intro_outro.py`)
本次最大的升级点在于其从静态展示到**动态状态机驱动**的转变。

1.  **动画流程 (State Machine):** `Start -> Entry -> Hold (智能计算) -> Exit -> Transition`
2.  **入场效果 (Entry - Flash Scale-In):**
    *   机制: 能量脉冲汇集。从屏幕深处的微光开始，以电光青/霓虹紫双色交织的脉冲快速放大。
    *   参数控制: 必须在 $T+0.5$ 秒内达到视觉冲击力的峰值，确保节奏感。
3.  **维持期 (Hold):**
    *   机制: 智能计算停留时长。系统根据内容信息密度（例如：高密度技术分析 vs. 低密度生活叙事）自动调整 Logo 展示时间 ($\text{Hold Min} \to \text{Hold Max}$)，防止视觉疲劳或留白过长。
4.  **退场效果 (Exit - Energy Dissolve):**
    *   机制: 采用“能量耗散”模型，Logo不会生硬消失。它会模拟由数据流构成的能量体，从全屏 $\to$ 收缩到核心光点 $\to$ 扩散波纹 $\to$ 像素化消散。
5.  **关键集成 (Seamless Transition):** 在 Logo 退场结束和主内容开始之间，必须插入一个 **过渡过载帧 (Transition Overload Frame)**。该帧通过数据流视觉瀑布模拟收尾的能量残留，确保了从动画到实拍素材的零接缝感。

### B. 封面生成器 (`cover_generator.py`)
1.  **视觉核心:** 所有文字元素必须具备“霓虹光晕发光”效果 (Glow Effect)。
2.  **技术实现要求:** 渲染管线中必须加入 **[Gaussian Blur + Color Shift]** 后处理滤镜。这使得所有文本不再是平面色块，而是拥有光源和体积感。
3.  **排版原则:** 利用主辅色的对比度（紫 vs 青）来建立信息层级，确保核心关键词始终处于视觉焦点。

---

## 🛠️ III. 系统流程总结与维护指导 (SOP)

*   **业务规则来源:** 所有功能逻辑和业务决策必须追溯至 [Project_Analysis_Doc.md] 中定义的初始模型。
*   **可扩展性保证:** 未来所有新增的动画或视觉元素，必须首先定义新的常量到 `branding_constants.py`，并检查其是否能被当前的 **状态机流程** 接受和管理。

***（End of Document）***